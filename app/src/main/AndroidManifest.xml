<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.harysuryanto.dinografi">

    <application
        android:allowBackup="true"
        android:icon="@drawable/dinosaur_drawing"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EncryptActivity" />
        <activity android:name=".DecryptActivity" />
        <activity android:name=".SavedCryptography" />
        <activity android:name=".DetailSavedCryptographyActivity"></activity>
    </application>

</manifest>